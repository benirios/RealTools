---
phase: 17-service-wiring-and-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/supabase.ts
  - tests/scoring-service.test.mjs
autonomous: true
requirements:
  - SCO-09
  - SCO-10
  - SCO-11
  - SCO-12
  - SCO-18
  - SCO-19
  - SCO-20

must_haves:
  truths:
    - "tests/scoring-service.test.mjs exists with stub test cases for all D-10 scenarios"
    - "types/supabase.ts Row/Insert/Update for opportunity_scores includes all 7 columns from migration 013"
    - "node --test tests/scoring-service.test.mjs runs without crashing (stubs may be skipped/todo)"
  artifacts:
    - path: "tests/scoring-service.test.mjs"
      provides: "Integration test file with stubs for D-10 test cases"
      min_lines: 80
    - path: "types/supabase.ts"
      provides: "Supabase generated types with opportunity_scores 013 columns"
      contains: "demographics_score"
  key_links:
    - from: "tests/scoring-service.test.mjs"
      to: "lib/scoring/data.js"
      via: "require('../lib/scoring/data.js')"
      pattern: "require.*lib/scoring/data"
    - from: "tests/scoring-service.test.mjs"
      to: "lib/scoring/service.js"
      via: "require('../lib/scoring/service.js')"
      pattern: "require.*lib/scoring/service"
---

<objective>
Wave 0: Remediate the types/supabase.ts gap from migration 013 and create the integration test stub file that Wave 1 and 2 plans will fill.

Purpose: Without types, every data.ts write will produce TypeScript errors on the 7 category columns. Without the test stub, later plans have no verification harness. Both blockers must be cleared before any other plan runs.

Output: Updated types/supabase.ts with all migration-013 columns, and tests/scoring-service.test.mjs with stubbed test descriptions for all D-10 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-service-wiring-and-api/17-CONTEXT.md
@.planning/phases/17-service-wiring-and-api/17-RESEARCH.md

<interfaces>
<!-- Migration 013 columns that must be added to types/supabase.ts -->
From supabase/migrations/013_opportunity_scores_category_columns.sql:
  demographics_score  NUMERIC(5,2)
  location_score      NUMERIC(5,2)
  foot_traffic_score  NUMERIC(5,2)
  competition_score   NUMERIC(5,2)
  risk_score          NUMERIC(5,2)
  investor_fit_score  NUMERIC(5,2)
  engine_version      TEXT NOT NULL DEFAULT '1.0'

Current types/supabase.ts Row for opportunity_scores (lines 587-601) is MISSING all 7 columns above.

From tests/scoring-engine.test.mjs — CJS import pattern to replicate:
  import { createRequire } from 'node:module'
  const require = createRequire(import.meta.url)
  const { ... } = require('../lib/scoring/engine.js')

D-10 test cases required as stubs:
  1. save/fetch round-trip succeeds
  2. missing location_insights returns Portuguese error in errors.general
  3. recompute of same listing+strategy produces score_version increment in the upsert payload
  4. getBestFitAction returns cafe as top strategy when cafe input data is stronger
  5. history endpoint returns the current score row (all strategies for listing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix types/supabase.ts — add migration-013 columns to opportunity_scores</name>
  <files>types/supabase.ts</files>
  <read_first>
    - types/supabase.ts (lines 586-650 — the full opportunity_scores Row/Insert/Update block)
    - supabase/migrations/013_opportunity_scores_category_columns.sql (exact column names and types)
  </read_first>
  <action>
First, attempt to regenerate types automatically:

  npx supabase gen types typescript --local > types/supabase.ts

If that command succeeds (local Supabase is running), verify that `demographics_score` appears in the output, then stop — types are correct.

If the command fails (local Supabase not running, exits non-zero), manually patch types/supabase.ts:

Locate the `opportunity_scores` block. In the `Row` object (currently ends after `user_id: string`), add these 7 fields before the closing `}`:

  competition_score: number | null
  demographics_score: number | null
  engine_version: string
  foot_traffic_score: number | null
  investor_fit_score: number | null
  location_score: number | null
  risk_score: number | null

In the `Insert` object, add the same 7 fields as optional:

  competition_score?: number | null
  demographics_score?: number | null
  engine_version?: string
  foot_traffic_score?: number | null
  investor_fit_score?: number | null
  location_score?: number | null
  risk_score?: number | null

In the `Update` object, add the same 7 fields as optional:

  competition_score?: number | null
  demographics_score?: number | null
  engine_version?: string
  foot_traffic_score?: number | null
  investor_fit_score?: number | null
  location_score?: number | null
  risk_score?: number | null

Do NOT alter any other table definitions. Do NOT reformat the file.
  </action>
  <verify>
    <automated>grep -n "demographics_score" /Users/beni/Dev/RealTools/types/supabase.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "demographics_score" types/supabase.ts` returns at least 3 lines (Row, Insert, Update)
    - `grep "engine_version" types/supabase.ts` returns at least 3 lines (Row, Insert, Update)
    - `grep "investor_fit_score" types/supabase.ts` returns at least 3 lines
    - `npx tsc --noEmit` exits 0 (or same errors as before — no new type errors introduced)
  </acceptance_criteria>
  <done>types/supabase.ts Row, Insert, and Update for opportunity_scores all include the 7 migration-013 columns with correct TypeScript types (number | null for NUMERIC, string for TEXT NOT NULL)</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/scoring-service.test.mjs with D-10 stub cases</name>
  <files>tests/scoring-service.test.mjs</files>
  <read_first>
    - tests/scoring-engine.test.mjs (CJS require pattern, fixture helpers, test structure — full file)
    - .planning/phases/17-service-wiring-and-api/17-CONTEXT.md (D-09, D-10 — exact test cases required)
    - .planning/phases/17-service-wiring-and-api/17-RESEARCH.md (mock Supabase pattern in "Code Examples" section)
  </read_first>
  <action>
Create tests/scoring-service.test.mjs as a new file.

Use these exact imports (mirrors scoring-engine.test.mjs CJS pattern):

  import assert from 'node:assert/strict'
  import test from 'node:test'
  import { createRequire } from 'node:module'

  const require = createRequire(import.meta.url)

Declare the require calls for the modules that will exist after Wave 1 executes. Wrap them in try/catch so the file can be parsed even before data.js and service.js exist:

  let upsertScore, getScoreHistory, getScore
  let scoreListingService

  try {
    ;({ upsertScore, getScoreHistory, getScore } = require('../lib/scoring/data.js'))
  } catch {
    // Wave 1 will create lib/scoring/data.js
  }
  try {
    ;({ scoreListingService } = require('../lib/scoring/service.js'))
  } catch {
    // Wave 1 will create lib/scoring/service.js
  }

Include these fixture helpers (replicate from scoring-engine.test.mjs pattern):

  function makeListing(overrides = {}) {
    return {
      id: 'listing-001', user_id: 'user-001', title: 'Ponto Comercial',
      source: 'olx', source_url: 'https://olx.com.br/1', country: 'BR',
      price_amount: 450000, lat: -8.12, lng: -34.90, tags: [],
      commercial_type: 'ponto_comercial', property_type: null,
      city: 'Recife', state: 'PE',
      ...overrides,
    }
  }

  function makeInsight(overrides = {}) {
    return {
      id: 'insight-001', userId: 'user-001', city: 'Recife', state: 'PE', country: 'BR',
      avgIncome: 4500, populationDensity: 6000, confidenceScore: 85,
      nearbyBusinesses: [
        { name: 'Escola', category: 'school', distanceMeters: 200, source: 'google' },
        { name: 'Shopping', category: 'mall', distanceMeters: 350, source: 'google' },
      ],
      dataSources: [], consumerProfile: null, latitude: -8.12, longitude: -34.90,
      rawGeocode: {}, rawDemographics: {}, rawPlaces: {},
      createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(),
      ...overrides,
    }
  }

  function makeScoreRow(overrides = {}) {
    return {
      id: 'score-001', user_id: 'user-001', listing_id: 'listing-001',
      strategy_slug: 'cafe', total_score: 72, score_version: 1,
      breakdown: [], signals: [], risks: [], fit_label: 'forte',
      computed_at: new Date().toISOString(),
      created_at: new Date().toISOString(), updated_at: new Date().toISOString(),
      demographics_score: 65, location_score: 70, foot_traffic_score: 62,
      competition_score: 68, risk_score: 75, investor_fit_score: 70,
      engine_version: '1.0',
      ...overrides,
    }
  }

Then create stub test blocks (use `test.todo` so they are skipped until Wave 3 fills them in):

  test.todo('SCO-18 save/fetch round-trip: upsertScore then getScoreHistory returns the saved row')
  test.todo('SCO-18 missing location_insights: scoreListingService returns Portuguese error in errors.general')
  test.todo('SCO-11/SCO-20 recompute increments score_version: upsertScore reads existing version and increments by 1')
  test.todo('SCO-19 getBestFitAction: cafe scores higher than logistics when nearbyBusinesses favor cafe')
  test.todo('SCO-10/SCO-20 getScoreHistory returns current row for listing (all strategy variants)')

Also add one concrete test that verifies the file itself loads without error:

  test('scoring-service test file loads', () => {
    assert.ok(true, 'test harness initialised')
  })
  </action>
  <verify>
    <automated>node --test tests/scoring-service.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/scoring-service.test.mjs` exits 0 (the one concrete test passes; todo stubs are reported as skipped, not failures)
    - File contains `test.todo('SCO-18 save/fetch round-trip` (grep verifiable)
    - File contains `test.todo('SCO-19 getBestFitAction` (grep verifiable)
    - File contains `test.todo('SCO-11/SCO-20 recompute increments score_version` (grep verifiable)
    - File contains `makeScoreRow` fixture helper (grep verifiable)
    - File contains `require('../lib/scoring/data.js')` (grep verifiable — indicates Wave 1 contract)
    - File contains `require('../lib/scoring/service.js')` (grep verifiable)
  </acceptance_criteria>
  <done>tests/scoring-service.test.mjs exists, runs without crashing, has all 5 D-10 stubs as test.todo entries, and includes fixture helpers (makeListing, makeInsight, makeScoreRow) for Wave 3 to fill in</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → lib/scoring/*.js | CJS require loads compiled modules; if wrong version loaded, tests verify wrong code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Spoofing | types/supabase.ts manual patch | accept | Type-only change; no runtime auth code; patch is additive-only |
| T-17-04 | Information Disclosure | test fixture makeScoreRow | accept | Fixtures use fake user-001/listing-001 IDs; no real PII; test-only file |
</threat_model>

<verification>
After both tasks complete:

1. `grep "demographics_score" types/supabase.ts | wc -l` must return 3 or more
2. `grep "engine_version" types/supabase.ts | wc -l` must return 3 or more
3. `node --test tests/scoring-service.test.mjs` exits 0
4. `npm test` must not regress (scoring-engine tests still 15/15)
</verification>

<success_criteria>
- types/supabase.ts Row, Insert, Update for opportunity_scores include all 7 migration-013 columns
- tests/scoring-service.test.mjs runs (1 pass, 5 todo)
- npm test output shows no new failures vs. Phase 16 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-wiring-and-api/17-01-SUMMARY.md`
</output>

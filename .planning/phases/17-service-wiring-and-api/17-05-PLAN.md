---
phase: 17-service-wiring-and-api
plan: 05
type: execute
wave: 3
depends_on:
  - 17-02
  - 17-03
files_modified:
  - app/api/listings/[id]/score/route.ts
autonomous: true
requirements:
  - SCO-09
  - SCO-10
  - SCO-11
  - SCO-12

must_haves:
  truths:
    - "POST /api/listings/[id]/score calls getUser() at top before any other logic"
    - "POST validates strategy_slug presence and type in request body"
    - "POST validates strategy_slug against STRATEGY_SLUGS const (T-17-02)"
    - "POST calls scoreListingService and returns 200 with score, or 422 with error message"
    - "GET /api/listings/[id]/score calls getUser() at top before any other logic"
    - "GET reads ?strategy query param and passes to getScoreHistory as optional filter"
    - "GET returns {scores: []} — array even when empty — never 404 for missing score"
    - "Both handlers return 401 JSON for unauthenticated requests (not redirect)"
    - "POST calls revalidateTag('opportunity_score') after successful score computation"
  artifacts:
    - path: "app/api/listings/[id]/score/route.ts"
      provides: "GET and POST handlers for score retrieval and computation"
      exports: ["GET", "POST"]
      min_lines: 60
  key_links:
    - from: "app/api/listings/[id]/score/route.ts"
      to: "lib/scoring/service.ts"
      via: "scoreListingService(supabase, user.id, id, strategySlug)"
      pattern: "scoreListingService"
    - from: "app/api/listings/[id]/score/route.ts"
      to: "lib/scoring/data.ts"
      via: "getScoreHistory(supabase, user.id, id, strategy)"
      pattern: "getScoreHistory"
---

<objective>
Wave 3, Plan B (parallel with Plan 04): Implement app/api/listings/[id]/score/route.ts — the HTTP API surface for score computation (POST) and score retrieval (GET).

Purpose: External API consumers and the Phase 18 fetch layer use these endpoints. The GET endpoint lets the UI fetch saved scores without triggering recompute. The POST endpoint triggers full computation via scoreListingService.

Output: app/api/listings/[id]/score/route.ts with GET and POST handlers, both enforcing getUser() auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-service-wiring-and-api/17-CONTEXT.md
@.planning/phases/17-service-wiring-and-api/17-RESEARCH.md
@.planning/phases/17-service-wiring-and-api/17-02-SUMMARY.md
@.planning/phases/17-service-wiring-and-api/17-03-SUMMARY.md

<interfaces>
<!-- Pattern to mirror exactly: app/api/listings/[id]/location-insight/route.ts -->
From app/api/listings/[id]/location-insight/route.ts:
  import { NextResponse } from 'next/server'
  import { createSupabaseServerClient } from '@/lib/supabase/server'
  type RouteContext = { params: Promise<{ id: string }> }
  export async function GET(_request: Request, { params }: RouteContext) {
    const { id } = await params   // MUST await params — Next.js 15 dynamic route params are async
    const supabase = await createSupabaseServerClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
    ...
    return NextResponse.json({ insight })
  }

Key: params is Promise<{id: string}> — must await before accessing id.
Key: API routes return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }) — NOT redirect('/auth/login')
Key: Only revalidateTag in route handlers — NOT revalidatePath (server action primitive)

From lib/scoring/data.ts (Plan 02 output):
  getScoreHistory(supabase, userId, listingId, strategySlug?): Promise<OpportunityScoreRow[]>
  // strategySlug is optional — if absent, returns all strategies for listing

From lib/scoring/service.ts (Plan 03 output):
  scoreListingService(supabase, userId, listingId, strategySlug): Promise<ScoringActionState & {scoreResult?: ScoreResult}>
  // result.errors present means failure; result.score present means success

From lib/scoring/strategies.ts:
  STRATEGY_SLUGS: readonly ['cafe','logistics','pharmacy','retail','services','any']
  // validate POST body strategy_slug against this

SCO-09 API spec: POST body { strategy_slug: string } -> returns { score: ScoreResult }
SCO-10 API spec: GET ?strategy=cafe -> returns { scores: OpportunityScoreRow[] }
                 GET without ?strategy -> returns { scores: OpportunityScoreRow[] } (all strategies)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/api/listings/[id]/score/route.ts — GET and POST handlers</name>
  <files>app/api/listings/[id]/score/route.ts</files>
  <read_first>
    - app/api/listings/[id]/location-insight/route.ts (full file — exact Next.js 15 route handler pattern: await params, getUser(), NextResponse.json)
    - lib/scoring/service.ts (scoreListingService signature — Plan 03 output)
    - lib/scoring/data.ts (getScoreHistory signature — Plan 02 output)
    - lib/scoring/strategies.ts (STRATEGY_SLUGS const — for POST body validation)
    - .planning/phases/17-service-wiring-and-api/17-CONTEXT.md (D-14, D-15, D-16)
    - .planning/phases/17-service-wiring-and-api/17-RESEARCH.md (Pattern 4: API Route code example)
  </read_first>
  <behavior>
    - GET: await params -> getUser() -> if null return 401 -> getScoreHistory(supabase, user.id, id, strategy?) -> return {scores: [...]}
    - POST: await params -> getUser() -> if null return 401 -> parse body -> validate strategy_slug present and string -> validate against STRATEGY_SLUGS -> call scoreListingService -> if errors return 422 -> revalidateTag -> return {score: result.score}
    - POST missing/invalid body: return 400 with {error: 'strategy_slug is required'}
    - POST invalid strategy_slug value: return 400 with {error: 'Estratégia inválida.'}
    - POST service error: return 422 with {error: result.errors.general[0]}
    - GET always returns {scores: array} — never 404 for empty results
  </behavior>
  <action>
First, create the directory if it doesn't exist:
  mkdir -p app/api/listings/[id]/score

Then create app/api/listings/[id]/score/route.ts with this exact content:

  import { NextResponse } from 'next/server'
  import { revalidateTag } from 'next/cache'
  import { createSupabaseServerClient } from '@/lib/supabase/server'
  import { scoreListingService } from '@/lib/scoring/service'
  import { getScoreHistory } from '@/lib/scoring/data'
  import { STRATEGY_SLUGS } from '@/lib/scoring/strategies'

  type RouteContext = { params: Promise<{ id: string }> }

  export async function GET(request: Request, { params }: RouteContext) {
    const { id } = await params
    const { searchParams } = new URL(request.url)
    const strategy = searchParams.get('strategy') ?? undefined

    const supabase = await createSupabaseServerClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

    const scores = await getScoreHistory(supabase, user.id, id, strategy)
    return NextResponse.json({ scores })
  }

  export async function POST(request: Request, { params }: RouteContext) {
    const { id } = await params

    const supabase = await createSupabaseServerClient()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

    const body = await request.json().catch(() => ({}))
    const strategySlug = body?.strategy_slug

    if (!strategySlug || typeof strategySlug !== 'string') {
      return NextResponse.json({ error: 'strategy_slug is required' }, { status: 400 })
    }

    // T-17-02: validate against allowed slugs before passing to engine
    if (!STRATEGY_SLUGS.includes(strategySlug as typeof STRATEGY_SLUGS[number])) {
      return NextResponse.json({ error: 'Estratégia inválida.' }, { status: 400 })
    }

    const result = await scoreListingService(supabase, user.id, id, strategySlug)

    if (result.errors) {
      return NextResponse.json(
        { error: result.errors.general?.[0] ?? 'Não foi possível calcular a pontuação.' },
        { status: 422 }
      )
    }

    revalidateTag('opportunity_score')
    return NextResponse.json({ score: result.score })
  }

Note on directory: app/api/listings/[id]/ already exists (location-insight route is there). Only the /score/ subdirectory needs to be created.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/api/listings.*score" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces zero errors mentioning app/api/listings/[id]/score/route.ts
    - `grep "await params" app/api/listings/\[id\]/score/route.ts` returns 2 lines (one per handler)
    - `grep "getUser()" app/api/listings/\[id\]/score/route.ts` returns 2 lines (one per handler)
    - `grep "status: 401" app/api/listings/\[id\]/score/route.ts` returns 2 lines
    - `grep "STRATEGY_SLUGS" app/api/listings/\[id\]/score/route.ts` returns 2+ lines (import + validation)
    - `grep "Estratégia inválida" app/api/listings/\[id\]/score/route.ts` returns 1 line
    - `grep "revalidateTag" app/api/listings/\[id\]/score/route.ts` returns 1 line (POST only — GET does not mutate)
    - `grep "revalidatePath" app/api/listings/\[id\]/score/route.ts` returns 0 lines (server action primitive, not route handler)
    - `grep "status: 422" app/api/listings/\[id\]/score/route.ts` returns 1 line
    - `grep "status: 400" app/api/listings/\[id\]/score/route.ts` returns 2 lines (missing body + invalid slug)
    - `grep "^export async function GET\|^export async function POST" app/api/listings/\[id\]/score/route.ts | wc -l` returns 2
    - File does NOT contain 'getSession' anywhere
    - File does NOT contain 'redirect(' anywhere (routes return 401 JSON, not redirect)
  </acceptance_criteria>
  <done>app/api/listings/[id]/score/route.ts exists with GET and POST handlers, both enforce getUser() returning 401 JSON for unauthenticated requests, POST validates strategy_slug against STRATEGY_SLUGS, revalidateTag is called only in POST after successful computation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → GET /api/listings/[id]/score | id and strategy query param come from untrusted external caller |
| HTTP client → POST /api/listings/[id]/score | id and strategy_slug body field come from untrusted external caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Elevation of Privilege | GET handler, POST handler | mitigate | `supabase.auth.getUser()` at top of each handler before any DB operation. Returns {error:'Unauthorized'} with HTTP 401 for unauthenticated requests. No data exposed before auth check. |
| T-17-01 | Spoofing | GET getScoreHistory call | mitigate | getScoreHistory receives `user.id` from getUser() — not from request params. Route param `id` is listingId only. Service layer additionally filters by user_id. RLS is DB backstop. |
| T-17-02 | Tampering | POST strategy_slug body field | mitigate | Two-layer validation: (1) typeof check for presence/type, (2) STRATEGY_SLUGS.includes() check for valid enum value. DB CHECK constraint on strategy_slug is tertiary backstop. |
| T-17-04 | Information Disclosure | GET handler with unstable_cache | mitigate | getScoreHistory wraps unstable_cache with userId as first key discriminator — User A cannot receive User B's cached scores. Cache tags are invalidated on every mutation via revalidateTag('opportunity_score'). |
</threat_model>

<verification>
After task completes:
1. `npx tsc --noEmit` exits 0 (zero errors on the route file)
2. `npm test` shows no regressions
3. Manual grep checks all pass (see acceptance_criteria above)
4. Manual verification (optional, requires running server):
   - `curl -X POST http://localhost:3000/api/listings/test/score -H 'Content-Type: application/json' -d '{"strategy_slug":"cafe"}' -v` returns 401 Unauthorized (no auth cookie)
   - `curl http://localhost:3000/api/listings/test/score?strategy=cafe -v` returns 401 Unauthorized
</verification>

<success_criteria>
- app/api/listings/[id]/score/route.ts compiles cleanly
- GET and POST both enforce getUser() returning 401 for unauthenticated requests
- POST validates strategy_slug (presence + STRATEGY_SLUGS membership)
- POST returns 422 when service returns error
- GET returns {scores: []} — always an array, never 404
- revalidateTag called only in POST (not GET, not via revalidatePath)
</success_criteria>

<output>
After completion, create `.planning/phases/17-service-wiring-and-api/17-05-SUMMARY.md`
</output>

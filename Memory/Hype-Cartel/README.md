# Memory Vault (active)

This folder is the canonical memory vault for the project. The deprecated `CopilotMemory` folder has been moved here as `Memory/CopilotMemory_deprecated` for archival.

Backlinks and important files:
- PROJECT_CONTEXT.md — consolidated project summary and PDF findings (moved here from repo root).
- Files/ — course PDFs and extracted materials (already present).
- CopilotMemory_deprecated/ — legacy Copilot memory archive (deprecated).

Purpose:
Store contextual documents, course materials, and assistant-generated summaries to improve contextual responses and preserve important artifacts.

If you want this merged into `contex.md`, confirm and the assistant will update that file too.

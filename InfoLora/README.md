# Infolora Backend

Scaffolded Node.js + TypeScript backend with Prisma (SQLite). Endpoints:
- GET /temperatures
- POST /webhooks/ttn
- GET /announcements
- POST /announcements

Run:
- npm run dev
- npm run simulate
